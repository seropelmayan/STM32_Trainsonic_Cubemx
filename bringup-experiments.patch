diff --git a/Inc/as5047.h b/Inc/as5047.h
index b68e020..3ccbc8f 100644
--- a/Inc/as5047.h
+++ b/Inc/as5047.h
@@ -35,7 +35,7 @@
    "inverted" setting to give a positive speed sign for positive torque. If the
    SDK reports inverted speed or encoder alignment fails during bring-up, flip
    this (0 <-> 1) and rebuild. */
-#define AS5047_INVERT_DIR     0
+#define AS5047_INVERT_DIR     1
 
 /* ------------------------------------------------------------------------- */
 /* Status                                                                    */
diff --git a/Inc/pmsm_motor_parameters.h b/Inc/pmsm_motor_parameters.h
index 9f29322..4193156 100644
--- a/Inc/pmsm_motor_parameters.h
+++ b/Inc/pmsm_motor_parameters.h
@@ -28,7 +28,7 @@
  ************************/
 
 /***************** MOTOR ELECTRICAL PARAMETERS  ******************************/
-#define POLE_PAIR_NUM           40 /* Number of motor pole pairs */
+#define POLE_PAIR_NUM           20 /* Number of motor pole pairs */
 #define RS                      0.74 /* Stator resistance , ohm*/
 #define LS                      0.002 /* Stator inductance, H
                                                  For I-PMSM it is equal to Lq */
diff --git a/Ropetow.ioc.wb b/Ropetow.ioc.wb
index 38fb45e..35ad4e6 100644
--- a/Ropetow.ioc.wb
+++ b/Ropetow.ioc.wb
@@ -1053,7 +1053,7 @@ MotorControl.M1_PID_SPEED_KP_DEFAULT=2560590
 MotorControl.M1_PID_TORQUE_KD_DEFAULT=100
 MotorControl.M1_PID_TORQUE_KI_DEFAULT=14
 MotorControl.M1_PID_TORQUE_KP_DEFAULT=590
-MotorControl.M1_POLE_PAIR_NUM=40
+MotorControl.M1_POLE_PAIR_NUM=20
 MotorControl.M1_POSITION_CTRL_ENABLING=false
 MotorControl.M1_POTENTIOMETER_ENABLE=false
 MotorControl.M1_POWERBOARD_NAME=RopeTow
diff --git a/Src/main.c b/Src/main.c
index 9b962fb..83abe0e 100644
--- a/Src/main.c
+++ b/Src/main.c
@@ -36,9 +36,10 @@
 
 /* Private define ------------------------------------------------------------*/
 /* USER CODE BEGIN PD */
+#define MOTOR_AUTO_START        1       /* 0 = diagnostic build: never start   */
 #define BOOT_TARGET_SPEED_RPM   50      /* automatic start target speed       */
 #define BOOT_SPEED_RAMP_MS      2000    /* 0 -> target ramp duration (ms)      */
-#define MOTOR_LOG_PERIOD_MS     1000U   /* periodic motor status log interval  */
+#define MOTOR_LOG_PERIOD_MS     500U    /* periodic motor status log interval  */
 /* USER CODE END PD */
 
 /* Private macro -------------------------------------------------------------*/
@@ -146,6 +147,10 @@ int main(void)
    * Only start if the gate driver and encoder configured cleanly. The start
    * is asynchronous: alignment briefly energises and may twitch the rotor,
    * then the speed ramp runs. The MC tasks advance from interrupts. */
+#if !MOTOR_AUTO_START
+  LOG_Printf("AUTO-START DISABLED (diagnostic build).\r\n");
+  LOG_Printf("Turn the shaft by hand and watch T3CNT / SPI / elAng.\r\n");
+#else
   if ((drv_st == DRV8353_OK) && (enc_st == AS5047_OK))
   {
     HAL_Delay(500);                          /* let the supply settle         */
@@ -169,6 +174,7 @@ int main(void)
     LOG_Printf("Motor start SKIPPED: drv_st=%d enc_st=%d\r\n",
                (int)drv_st, (int)enc_st);
   }
+#endif /* MOTOR_AUTO_START */
   /* USER CODE END 2 */
 
   /* Infinite loop */
@@ -674,14 +680,21 @@ static void motor_status_log(void)
   last_state  = state;
   last_faults = faults;
 
-  LOG_Printf("[motor] %s spd=%ld/%ld rpm I=%.1fA Vbus=%uV enc=%s\r\n",
+  /* Current as integer mA: %f is unavailable (no float printf in this build). */
+  int32_t i_ma = (int32_t)((float)MC_GetPhaseCurrentAmplitudeMotor1() *
+                           (float)CURRENT_CONV_FACTOR_INV * 1000.0f);
+
+  LOG_Printf("[motor] %s spd=%ld/%ld rpm I=%ldmA Vbus=%uV enc=%s "
+             "| T3CNT=%lu SPI=%u elAng=%d\r\n",
              mc_state_name(state),
              (int32_t)MC_GetMecSpeedAverageMotor1()   * U_RPM / SPEED_UNIT,
              (int32_t)MC_GetMecSpeedReferenceMotor1() * U_RPM / SPEED_UNIT,
-             (double)((float)MC_GetPhaseCurrentAmplitudeMotor1() *
-                      (float)CURRENT_CONV_FACTOR_INV),
+             i_ma,
              (unsigned)VBS_GetAvBusVoltage_V(&BusVoltageSensor_M1._Super),
-             MC_GetSpeedSensorReliabilityMotor1() ? "ok" : "BAD");
+             MC_GetSpeedSensorReliabilityMotor1() ? "ok" : "BAD",
+             (uint32_t)htim3.Instance->CNT,
+             (unsigned)AS5047_ReadAngle(&hspi1, NULL),
+             (int)MC_GetElAngledppMotor1());
 
   if (changed && ((faults != 0U) || (MC_GetOccurredFaultsMotor1() != 0U)))
   {
